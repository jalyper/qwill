{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Qwill main window. User documents go through the read_document/write_document commands (.docx only); the fs plugin is limited to Qwill's own app-data folder (drafts, workspace state).",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    "core:window:allow-destroy",
    "core:window:allow-set-title",
    "dialog:allow-open",
    "dialog:allow-save",
    "dialog:allow-ask",
    "dialog:allow-message",
    "fs:allow-appdata-read-recursive",
    "fs:allow-appdata-write-recursive",
    "fs:allow-appdata-meta-recursive"
  ]
}
